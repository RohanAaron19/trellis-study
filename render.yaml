services:
  - type: web
    name: trellis-study
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    disk:
      name: trellis-data
      mountPath: /data
      sizeGB: 1
